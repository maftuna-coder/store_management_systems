This project is Store Management System. its tasks are:
 *firstly user input product, price, points for each product and product code, quantity
 *program calculates total price, quantity and points
 *it shows results with those inputs in new window as for user
 *this program can check invalid value input and give chance user to improve mistake
 *it can avoid any crash of software